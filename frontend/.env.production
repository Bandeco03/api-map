# Backend API URL for production
# Since backend and frontend are on the SAME PC, use empty string to make requests relative
# When using ngrok, the Vite proxy will forward /api requests to localhost:8000
VITE_API_URL=

